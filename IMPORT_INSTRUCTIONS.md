# Інструкції по імпорту товарів на Render

## ⚠️ ВАЖЛИВО

**Імпорт товарів НЕ виконується автоматично під час деплою**, щоб уникнути таймаутів білду.

## 🚀 Після деплою на Render

### Крок 1: Відкрити Render Shell

1. Зайти на https://dashboard.render.com
2. Вибрати сервіс `django-shop`
3. Перейти в `Shell` (права панель)

### Крок 2: Виконати імпорт товарів

```bash
cd ~/project/src
bash import_products_render.sh
```

Це займе ~5-10 хвилин і завантажить всі товари з постачальника.

## 📋 Що імпортується автоматично під час білду

✅ Категорії (швидко)
✅ Відгуки (швидко)
❌ Товари (повільно, ~5-10 хв) - запускати вручну!

## 🔄 Автоматична синхронізація

Після першого імпорту, товари будуть автоматично оновлюватись через cron job кожні 2 години.

## 🛠️ Альтернативний спосіб (через Django shell)

```bash
cd ~/project/src
python manage.py import_products --batch-size 50
```

## ✅ Перевірка результатів

```bash
python manage.py shell -c "from apps.products.models import Product; print(f'Товарів: {Product.objects.count()}')"
```

